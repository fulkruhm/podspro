steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - .
      - -f
      - Dockerfile
      - --target
      - frontend-prod
      - -t
      - $_IMAGE_URI

images:
  - $_IMAGE_URI
